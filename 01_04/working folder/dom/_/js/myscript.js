//console.log('Hey There');
//console.log(document.querySelector("#main"));
//console.dir(document.querySelector("#main"));
//console.info("some message");
//console.warn("Some Message");
//console.error("Some Message");

// these go together
	//console.group("Page Links");
	//console.dir(document.querySelectorAll('a'));
	//console.groupEnd();
	
// these go together
	//console.groupCollapsed("Paragraphs");
	//console.dir(document.querySelectorAll('p'));
	//console.groupEnd();

// these go together	
	//console.time("BigLoop");
		//for (var i=1000000; i >=0; i--) {
		//};
	//console.timeEnd("BigLoop");

// these go together	
	//console.assert(
		//document.querySelectorAll('nav ol>li').length === 2, // this will give us an assertion error
		//"Sorry, there's only two menu items"
	//);
	
// these go together
	//console.assert(
		//document.querySelectorAll('nav ol>li').length === 3,
		//"Sorry, theirs only two menu items."
	//);	
