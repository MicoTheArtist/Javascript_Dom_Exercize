// NOTE: AppendBefore is even better than appendChild ("index.php" page)
	// var pNode = document.createElement('p')
	// pNode
	// var myText = document.createTextNode('within walking distance to majore shopping venue')
	// myText
	// pNode.appendChild(myText)
	// pNode
	// var newNode = document.querySelector('#thevenue')
	// newNode.insertBefore(pNode, newNode.childNodes[5])